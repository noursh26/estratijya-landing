Use `ValueRow` for the values list and the "what sets us apart" list — the brand's signature two-part sentence.

```jsx
<ValueRow label="Clarity" body="We say what we found, not what you want to hear." />
```

Stack them with a `Divider` between rows. Centre-align on the values slide, start-align on web.
