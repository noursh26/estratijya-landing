Use `PartnerChip` for the scrolling client rail.

```jsx
<PartnerChip name="Royal Golf" />
```

The brand ships no partner logo files, so names are set in type inside a hairline pill. If real logos arrive, swap the chip contents for the mark and keep the pill.
