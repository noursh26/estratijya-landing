Use `StatBlock` for the proof numbers under a hero ("Active clients", "Integrated divisions").

```jsx
<StatBlock prefix="+" value="40" label="Active clients" onDark />
```

Group two to four in a row separated by vertical `Divider`s. Never invent metrics — the brand only publishes numbers it measured.
