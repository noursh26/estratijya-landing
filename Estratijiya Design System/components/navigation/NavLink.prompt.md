Use `NavLink` for the four header links (About, Services, Results, Contact).

```jsx
<NavLink href="#about" active>About</NavLink>
```

Labels are single words wherever possible. The underline is the only active affordance — never a pill or background.
