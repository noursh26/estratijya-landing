Use `SectionHeader` to open every section so the numbered rhythm stays intact.

```jsx
<SectionHeader number="04" eyebrow="Our services" title="Seven services. One system." lede="Not a menu to pick from — one integrated journey." onDark />
```

Headings are short declaratives, often two sentences fused by a full stop ("We study. We build the strategy. Then we execute.").
