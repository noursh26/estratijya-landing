Use `Eyebrow` for the small green label that names a section ("The method", "Who we work with").

```jsx
<Eyebrow onDark>What sets us apart</Eyebrow>
```

Sentence-case source copy is rendered uppercase with `--tracking-wide`. Never longer than four words.
