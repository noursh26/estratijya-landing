Use `Divider` between stacked list rows (values, footer links, contact rows).

```jsx
<Divider onDark />
```

Never use a thicker rule or a coloured one; separation elsewhere comes from space and ground colour.
