Use `SectionNumber` at the top of every page section — the brand numbers its sections 01…08 in sequence.

```jsx
<SectionNumber value="03" onDark />
```

Pair it with an `Eyebrow` on the same row, then the section heading below.
